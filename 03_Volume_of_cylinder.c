#include<stdio.h>

int main(){
        // FINDING VOLUME OF CYLINDER
    int r = 5;
    int height = 10;
    printf("The area of circle with radius %d is found to be: %f", r, 3.14*r*r);
    printf("\nThe volume of cylinder with radius %d and height %d is found to be: %f", r,height, 3.14*r*r*height);
    return 0;
}